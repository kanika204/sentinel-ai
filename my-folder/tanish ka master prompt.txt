You are a senior Python, Kafka, Airflow, Docker, and AIOps engineer.

Work directly in the current repository and complete the entire AIOps Lab project in one go. Inspect the repository first and preserve any existing work. Implement all five questions below with clean, maintainable, type-hinted Python code.

General requirements:
- Use environment variables for brokers, topics, thresholds, ports, retries, and IDs.
- Validate all incoming data before processing.
- Use structured logging instead of scattered print statements.
- Handle malformed messages, Kafka failures, retries, and clean shutdowns.
- Keep modules independently testable.
- Follow PEP 8.
- Do not expose secrets.
- Update README.md with synchronized setup and execution instructions.
- Update requirements.txt with compatible dependencies only.
- Use Docker Compose for reproducible Kafka setup.
- Run focused validation after each question.
- Commit each completed question separately on the main branch.
- Use these exact commit messages:
  1. Created and fetched anomalies successfully
  2. Kafka produces is successfully created
  3. kafka consumer is successfully created
  4. airflow setup and dag completed
  5. integrated aiops monitor completed
- Generate and commit the Question 1 chart as:
  include anomaly detection chart
- Push all commits to origin/main after completion.
- Do not stop for confirmation between questions.
- Before editing, inspect the repository and existing Git history.
- Do not recreate files or duplicate commits that already exist.
- Continue automatically unless a real environment blocker occurs.
- If a blocker occurs, explain it clearly, complete all non-blocked work, and report the exact failed command.

Question 1: AIOps Log Anomaly Detection

Create q1_anomaly_detection.py that:

1. Creates or reads a deterministic sample dataset containing 20 server records.
2. Includes timestamp, CPU usage, memory usage, and response time.
3. Calculates average, minimum, maximum, and standard deviation statistics.
4. Detects CPU anomalies using a threshold-based approach.
5. Uses 80% as the default CPU anomaly threshold.
6. Prints the total records, anomaly count, timestamp, CPU, and status.
7. Ensures the three expected anomalies are:
   - 10:05 with CPU 95%
   - 10:12 with CPU 97%
   - 10:18 with CPU 92%
8. Creates a matplotlib chart showing CPU values, threshold, and anomalies.
9. Saves the chart as q1_anomalies.png.
10. Adds reusable functions and type hints.
11. Runs headlessly during validation.
12. Commits the implementation and then commits q1_anomalies.png separately.

Question 2: Kafka Topic and Producer

Create q2_kafka_setup.py that:

1. Connects to Kafka using KAFKA_BOOTSTRAP_SERVERS.
2. Uses KAFKA_TOPIC with default server_metrics.
3. Creates the topic idempotently.
4. Supports configurable partitions and replication factor.
5. Retries while Kafka is starting.
6. Handles TopicAlreadyExistsError safely.
7. Logs failures clearly.

Create q2_kafka_producer.py that:

1. Generates at least 10 server metric messages.
2. Includes server_id, cpu_usage, memory_usage, disk_usage, and timestamp.
3. Publishes JSON messages to server_metrics.
4. Uses Kafka acknowledgements and retries.
5. Handles connection failures explicitly.
6. Supports configurable SERVER_ID, message count, and interval.
7. Supports SIGINT and SIGTERM clean shutdown.
8. Verifies successful publication.

Create docker-compose.yml that:

1. Runs a single-node Kafka broker in KRaft mode.
2. Uses a currently valid official Kafka image.
3. Exposes Kafka on localhost:9092.
4. Disables unwanted automatic topic creation.
5. Uses a persistent volume.
6. Includes all required environment variables.

Validate by starting Kafka, creating server_metrics, publishing 10 messages,
and consuming and validating all 10 messages with Python kafka-python.
Fix any Docker or connectivity issue found.

Question 3: Kafka Consumer

Create q3_kafka_consumer.py that:

1. Connects to the configured Kafka broker and consumes server_metrics.
2. Parses JSON safely and validates server_id, cpu_usage, memory_usage,
   disk_usage, and timestamp.
3. Rejects malformed JSON, missing fields, invalid types, and out-of-range values.
4. Logs every valid metric.
5. Emits exactly: ALERT: High CPU detected on server01 when CPU is greater than 80%.
6. Treats exactly 80% as normal.
7. Retries temporary Kafka errors.
8. Supports clean shutdown and bounded max_messages testing.
9. Validates against live Kafka messages.

Question 4: Airflow AIOps DAG

Create q4_airflow_aiops_dag.py that:

1. Defines a standard Airflow DAG using PythonOperator.
2. Defines exactly these tasks:
   collect_metrics, process_metrics, detect_anomaly, generate_report
3. Uses this exact chain:
   collect_metrics >> process_metrics >> detect_anomaly >> generate_report
4. collect_metrics generates CPU 87, memory 65, and response time 420ms.
5. Passes task data through XCom.
6. Validates and logs metrics in process_metrics.
7. Checks CPU > 80 in detect_anomaly and logs the appropriate result.
8. Logs a final AIOps report in generate_report.
9. Adds retries, retry delay, scheduling, catchup=False, and clear logging.
10. Uses environment variables for threshold and retry configuration.
11. Runs syntax and AST validation.
12. If Airflow is installed, imports and validates the DAG directly.
13. If Airflow is unavailable, reports the limitation and completes structural validation.

Question 5: Integrated AIOps Monitor

Create q5_integrated_aiops_monitor.py that:

1. Consumes metrics from server_metrics.
2. Reuses or matches Question 3 validation behavior.
3. Detects CPU anomalies when CPU > 80% and emits a high-CPU alert.
4. Maintains processed metric, running anomaly, and final anomaly counts.
5. Logs running totals after each valid message.
6. Logs a final report containing Total anomalies detected.
7. Handles malformed messages safely.
8. Retries temporary Kafka and broker startup failures.
9. Supports clean SIGINT and SIGTERM shutdown.
10. Supports max_messages for deterministic testing.
11. Validates with a fake consumer and live Kafka messages.
12. Confirms counts and alerts are correct.

Final verification:

- Compile every Python module.
- Run focused assertions for Questions 1, 3, and 5.
- Validate Docker Compose configuration.
- Start Kafka and run Kafka integration tests.
- Run git diff --check.
- Do not commit __pycache__ or unrelated generated files.
- Keep q1_anomalies.png because it is required.
- Commit all changes on main using the specified messages.
- Push main to origin.
- Show changed files, validation results, commit hashes, final Git status, and limitations.